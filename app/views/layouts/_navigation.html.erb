<div class="navbar navbar-default navbar-static-top">
  <div class="container">
    <%= link_to 'FinTrack', root_path, class: "navbar-brand" %>
    <% if current_user %>
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <div class="navbar-collapse collapse navbar-responsive-collapse">
        <ul class="nav navbar-nav col-md-6 p-0">
          <li><%= link_to "Portfolio", portfolio_path  %></li>
          <li><%= link_to "Profile", edit_registration_path(current_user)  %></li>
          <li><%= link_to "Link3", "/path3"  %></li>
        </ul>
        <ul class="col-md-6 nav navbar-right p-0">
          <li class="col-md-8 text-right pl-1">
            <i class="fa fa-user"></i>
            <%= current_user.full_name.present? ? current_user.full_name : current_user.email %>
          </li>
          <li class="col-md-4 p-0">
            <%= link_to "logout", destroy_user_session_path,
                        class: "label label-danger", method: :delete %>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</div>
